
add a quick booking button on the front page


Pseudocode:

Save Member Method (member):
    sql = "insert member attributes with VALUES and RETURN id"
    values = member attributes
    results = run_sql(sql, values)
    id = results[0][returned id]
    member assigned the returned    


def save(member):
    sql = "INSERT INTO members (first_name, last_name, premium, active) VALUES (%s, %s, %s, %s) RETURNING id"
    values = [member.first_name, member.last_name, member.premium, member.active]
    results = run_sql(sql, values)
    id = results[0]['id']
    member.id = id